#pragma once

#include "core/Enumeration.h"

namespace sweetPy
{
    class SerializeType
    {
    public:
        enum Enumeration
        {
            FlatBuffers
        };
        ENUMERATION(SerializeType)
    };
}
